# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock IS committed for apps (pins exact dependency versions).

# Secrets / build config (keep the OAuth Web client id out of source).
# Real values live in dart_defines.json; commit dart_defines.example.json only.
dart_defines.json

# IDE / tooling
.idea/
.vscode/
*.iml
*.log

# Android
android/.gradle/
android/local.properties
android/key.properties
**/*.keystore
**/*.jks

# iOS / macOS
ios/Pods/
ios/.symlinks/
**/Flutter/Flutter.framework/
.DS_Store

# Hive / generated
*.g.dart.bak
